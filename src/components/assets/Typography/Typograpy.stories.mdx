import { Meta, Typeset, ArgsTable } from "@storybook/addon-docs/blocks";
import { defaultTheme as theme } from "../../../utils/themes";
import Typography from "./Typography";

<Meta title="Design System/Assets/Typography" />

# Typography

<ArgsTable of={Typography} />

<Typeset
  fontSizes={[
    `${Number(theme.typography.size.h1)}px`,
    `${Number(theme.typography.size.h2)}px`,
    `${Number(theme.typography.size.h3)}px`,
    `${Number(theme.typography.size.h4)}px`,
    `${Number(theme.typography.size.h5)}px`,
    `${Number(theme.typography.size.h6)}px`,
    `${Number(theme.typography.size.large)}px`,
    `${Number(theme.typography.size.base)}px`,
    `${Number(theme.typography.size.sm)}px`,
    `${Number(theme.typography.size.xs)}px`,
  ]}
/>
