import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs/blocks";
import Button from "./Button";

<Meta title="Design System/Atoms/Button" component={Button} />

# Button

export const Template = args => <Button {...args} />;

<!-- export const Primary = args => <Button {...args}>{arg.children}</Button>;
 Primary.args = { primary: true, children: "Primary" }; -->

<ArgsTable of={Button} />

### All

<Canvas>
  <Story name="All">
    <Button>Default</Button>
    <Button primary>Primary</Button>
    <Button dark>Dark</Button>
    <Button secondary>Secondary</Button>
    <Button primary outline>
      Outline Primary
    </Button>
    <Button secondary outline>
      Outline Secondary
    </Button>
    <Button primary link href="https://google.com">
      Link Button Primary
    </Button>
  </Story>
  <Story name="Control" args={{ children: "Text" }}>
    {Template.bind({})}
  </Story>
</Canvas>

### Sizes

<Canvas>
  <Story name="Sizes">
    <Button primary size="sm">
      Small
    </Button>
    <br />
    <br />
    <Button primary>Medium</Button>
    <br />
    <br />
    <Button primary size="large">
      Large
    </Button>
    <br />
    <br />
    <Button primary size="wide">
      Wide
    </Button>
  </Story>
</Canvas>
