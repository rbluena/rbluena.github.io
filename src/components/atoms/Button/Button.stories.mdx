import { Meta, Story, Canvas, Typeset } from "@storybook/addon-docs/blocks";
import { defaultTheme as theme } from "../../../utils/themes";
import Button from "./Button";

<Meta title="Design System/Atoms/Button" component={Button} />

# Button

export const Template = args => <Button {...args} />;

<Canvas>
  <Story
    name="Default"
    args={{
      children: "Default",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Primary"
    args={{
      children: "Primary",
      primary: true,
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Secondary"
    args={{
      children: "Secondary",
      secondary: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
